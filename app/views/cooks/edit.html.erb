<h1>Editing Cook</h1>
 <% if @cook.errors.any? %>
  <%= @cook.errors.count %>errors prohibited this book from being saved:
  <% @cook.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>
<%= form_with model:@cook, local: true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Update Cook' %>
<% end %>
<td><%= link_to 'Show', cook_path %></td>
|
<td><%= link_to 'Back', cooks_path %></td>